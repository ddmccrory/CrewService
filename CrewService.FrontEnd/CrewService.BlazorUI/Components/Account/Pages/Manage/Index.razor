@page "/Account/Manage"

@inject IdentityRedirectManager RedirectManager

<PageTitle>Profile</PageTitle>

<h3>Profile</h3>

<p class="text-muted">Profile management will be available in a future release.</p>

@code {
    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;
}
